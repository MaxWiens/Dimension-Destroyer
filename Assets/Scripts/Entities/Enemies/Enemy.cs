using UnityEngine;
using UnityEngine.AI;

public class Enemy : MonoBehaviour {
	[SerializeField, NotNull] private NavMeshAgent _agent = default;

	public void FollowPlayer(){
		// if(_agent.SetDestination()){

		// }else{
		// 	b
		// }
	}
}